#include <stdlib.h>
#include <stdbool.h>
#include <stdio.h>

#include "main.h"
#include "queue.h"
#include "parser.h"

bool parse_buffer ( unsigned char* buffer ) 
{
    /*
    for(int i = 0 ; i < BUFFER_SIZE;i++)
    {
        printf("0x%x ",buffer[i]);
    }
    printf("\n");
    */
    return 1;
}
